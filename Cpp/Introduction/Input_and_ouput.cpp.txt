#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    
    int in1, in2, in3, coutOUT ;
    int sum = 0 ;
    cin >> in1 >> in2 >> in3  ;
    
    //for(int i=0; cinIN[i];i++)
    //{
        sum = in1 + in2 + in3 ;
    //}
    cout << sum << endl ;
    
    return 0;
}